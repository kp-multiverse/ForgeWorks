{
  "$comment": "Project hooks. Enforcement belongs in hooks, not prompts. The deps-guard PreToolUse hook enforces supply-chain checks deterministically; the tree-claim PreToolUse hook keeps a second session opened in the same directory from writing over the first. The static+test quality gate is enforced by the reviewer subagent's Stop hook (see .claude/agents/reviewer.md) and by CI.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/deps-guard.sh",
            "timeout": 30,
            "statusMessage": "Supply-chain guard: checking dependency commands..."
          }
        ]
      },
      {
        "matcher": "Edit|Write|NotebookEdit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/tree-claim.sh",
            "timeout": 15,
            "statusMessage": "Tree claim: checking this session owns the working tree..."
          }
        ]
      }
    ]
  }
}
