# Plans -- one per FEATURE

One file per FEATURE (`<feature-id>.md`): approach, riskiest
assumption + how it was de-risked, data shapes, test list, and the owner's
approval line (`Approved: YYYY-MM-DD`). Chores don't require plans; use the PR
description instead. Plans are moved to `docs/plans/archive/` at MERGE.
