/**
 * Placeholder module so the scaffold passes the quality gate from the first run.
 *
 * Delete this and `tests/example.test.ts` when you build your first real slice;
 * real code follows the conventions in `docs/language-standards.md` (one
 * concept per file, ~100-line target, 200-line hard cap).
 */

export function greet(name: string): string {
  return `Hello, ${name}!`;
}
